Made a script so we don't have to add google-modules 1 by 1. This will also update the tags for the modules to whatever you set it as in the script under branch. 
To run this script:
1. give it permission by chmod +x (whatever you named the script)
2. type ./(name of script) to run it
3. watch the magic happen
